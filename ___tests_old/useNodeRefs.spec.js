import useNodeRefs from '../dist/shared/internal_hooks/useNodeRefs';

// TODO: test this hook
describe('useNodeRefs hook', () => {
  it('should be a function', () => {
    expect(useNodeRefs).to.be.a('function');
  });
});
