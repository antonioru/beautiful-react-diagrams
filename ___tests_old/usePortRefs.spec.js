import usePortRefs from '../dist/shared/internal_hooks/usePortRefs';

// TODO: test this hook
describe('usePortRefs hook', () => {
  it('should be a function', () => {
    expect(usePortRefs).to.be.a('function');
  });
});
