import useCanvas from '../src/shared/hooks/useCanvas';

// TODO: test this hook
describe('useCanvas hook', () => {
  it('should be a function', () => {
    expect(useCanvas).to.be.a('function');
  });
});
